version: '3'
services:
  prometheus:
    image: prom/prometheus
    volumes:
      - "/mnt/container_data/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml"
    networks:
      - localprom
    ports:
      - 9090:9090
networks:
  localprom:
    driver: bridge
